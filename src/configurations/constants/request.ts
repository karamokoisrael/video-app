export const DEFAULT_SWR_CONFIG = {
  revalidateOnFocus: false,
  shouldRetryOnError: true,
};

export const APP_RESYNC_INTERVAL = 30000;
export const WEBSOCKET_RECONNECT_INTERVAL = 3000;
export const WEBSOCKET_MAX_TRIES = 3000;
